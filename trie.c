#include <stdio.h>
#include "trie.h"

void addTrie(Node** head, char* word) {

}



void print(const Node* head) {

}

int main() {
	char* worde = 0;
	while (scanf("%s", worde) != EOF) {
		fprintf("%c\n", worde);
	}
	return 0;
}